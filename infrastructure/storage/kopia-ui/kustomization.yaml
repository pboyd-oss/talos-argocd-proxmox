---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kopia-ui
resources:
  - namespace.yaml
  - sealedsecret.yaml
  - pvc.yaml
  - deployment.yaml
  - httproute.yaml

configMapGenerator:
  - name: kopia-ui-config
    files:
      - repository.config
