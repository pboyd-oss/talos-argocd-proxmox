apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: backstage-database
spec:
  bootstrap:
    initdb:
      database: backstage
      owner: backstage
      secret:
        name: backstage-app-secret
