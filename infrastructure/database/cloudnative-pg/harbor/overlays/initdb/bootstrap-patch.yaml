apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: harbor-database
spec:
  bootstrap:
    initdb:
      database: registry
      owner: harbor
      secret:
        name: harbor-app-secret
