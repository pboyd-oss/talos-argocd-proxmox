apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: harbor-database
spec:
  bootstrap:
    recovery:
      source: harbor-recovery-source
      database: registry
      owner: harbor
      secret:
        name: harbor-app-secret
      # No recoveryTarget → restore to latest-WAL available on S3.
      # See gitea/overlays/recovery/bootstrap-patch.yaml for rationale.
  externalClusters:
    - name: harbor-recovery-source
      barmanObjectStore:
        # Prior lineage to restore from. Bump to N-1 at DR time.
        serverName: harbor-database-v1
        destinationPath: s3://postgres-backups/cnpg/harbor
        endpointURL: http://192.168.100.2:30293
        s3Credentials:
          accessKeyId:
            name: cnpg-s3-credentials
            key: AWS_ACCESS_KEY_ID
          secretAccessKey:
            name: cnpg-s3-credentials
            key: AWS_SECRET_ACCESS_KEY
        wal:
          compression: gzip
