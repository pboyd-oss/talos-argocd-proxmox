apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: tunnel-credentials
  namespace: cloudflared
spec:
  encryptedData:
    credentials.json: REPLACE_ME
  template:
    metadata:
      name: tunnel-credentials
      namespace: cloudflared
