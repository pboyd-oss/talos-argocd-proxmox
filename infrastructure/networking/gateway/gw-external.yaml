apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-external
  namespace: gateway
  labels:
    external-dns: "true"
    external-dns-gateway: gateway-external
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-cluster-issuer
    # External-DNS: Create CNAME records to tuxgrid.com instead of A records
    external-dns.alpha.kubernetes.io/target: "tuxgrid.com"
spec:
  # No addresses - Cilium will auto-assign, but external-DNS will use target annotation for CNAME
  gatewayClassName: cilium
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.tuxgrid.com"
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.tuxgrid.com"
      tls:
        certificateRefs:
          - name: cert-tuxgrid
            kind: Secret
            group: "" # required
        # No QUIC/HTTP3 - Cloudflare Tunnel uses HTTP/2, not QUIC
        options:
          gateway.networking.k8s.io/alpn-protocols: "h2,http/1.1"
      allowedRoutes:
        namespaces:
          from: All