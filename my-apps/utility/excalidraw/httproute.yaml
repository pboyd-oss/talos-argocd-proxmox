# Public HTTPRoute via gateway-external (Cloudflare tunnel + external-dns).
# Publishes https://excalidraw.tuxgrid.com → excalidraw Service :80.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: excalidraw
  namespace: excalidraw
  labels:
    external-dns: "true"
  annotations:
    external-dns.alpha.kubernetes.io/target: tuxgrid.com
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: gateway-external
      namespace: gateway
      sectionName: https
  hostnames:
    - "excalidraw.tuxgrid.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: excalidraw
          port: 80
          weight: 1
