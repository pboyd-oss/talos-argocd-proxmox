apiVersion: v1
kind: Service
metadata:
  name: copyparty
  namespace: copyparty
spec:
  selector:
    app: copyparty
  ports:
    - protocol: TCP
      port: 3923
      targetPort: 3923
      name: http
