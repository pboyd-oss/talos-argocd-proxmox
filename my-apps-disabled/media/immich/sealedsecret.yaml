apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: immich-db-credentials
  namespace: immich
spec:
  encryptedData:
    username: REPLACE_ME
    password: REPLACE_ME
  template:
    metadata:
      name: immich-db-credentials
      namespace: immich
