apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kiwix-data
  namespace: kiwix
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: kiwix-smb
  resources:
    requests:
      storage: 100Gi
