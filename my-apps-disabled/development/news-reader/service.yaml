apiVersion: v1
kind: Service
metadata:
  name: news-reader
  namespace: news-reader
spec:
  selector:
    app: news-reader
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  type: ClusterIP
