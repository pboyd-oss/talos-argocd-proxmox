<clickhouse>
    <tcp_port>9000</tcp_port>

    <remote_servers>
        <posthog>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog>
        <posthog_single_shard>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_single_shard>
        <posthog_migrations>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_migrations>
        <posthog_writable>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_writable>
        <posthog_primary_replica>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_primary_replica>
        <!--
          ai_events — newer PostHog (image sha 9b518de) expects this cluster
          when running `migrate_clickhouse`. It's used by the AI/LLM event
          ingestion path which assumes a separate "satellite" ClickHouse
          cluster. On single-node installs we just point it at the same
          node; queries resolve transparently. Without this cluster, every
          migrate_clickhouse run fails with:
            DB::Exception: Requested cluster 'ai_events' not found (code 701)
          which cascades into the entire posthog-migrate PostSync hook
          hitting backoffLimit and Argo wedging the app sync.
        -->
        <ai_events>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </ai_events>
        <!--
          Satellite clusters — PostHog's `CLICKHOUSE_SATELLITE_CLUSTERS`
          defaults to `"ai_events,aux,ops,sessions"` (see
          posthog/settings/data_stores.py). All four must exist or
          `migrate_clickhouse` fails with
          `DB::Exception: Requested cluster '<name>' not found (code 701)`
          and cascades into the PostSync hook wedging Argo. On a single-
          node install we point every satellite at the same host; queries
          resolve transparently.
        -->
        <aux>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </aux>
        <ops>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </ops>
        <sessions>
            <shard>
                <replica>
                    <host>clickhouse.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </sessions>
    </remote_servers>

    <!--
      named_collections — kept in sync with upstream
      PostHog/posthog:docker/clickhouse/config.d/default.xml. All three
      collections just point at $KAFKA_HOSTS; they exist as aliases so
      Kafka-engine table DDL in migrations can reference
      `kafka(warpstream_calculated_events, ...)` instead of hard-coding
      brokers. Missing `warpstream_calculated_events` is what broke
      migrate_clickhouse on 2026-04-19 after PostHog PR #53820 (commit
      fccbc6b92e, 2026-04-09) added the 0227_warpstream migration.
    -->
    <named_collections>
        <msk_cluster>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </msk_cluster>
        <warpstream_ingestion>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_ingestion>
        <warpstream_calculated_events>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_calculated_events>
    </named_collections>

    <macros>
        <shard>01</shard>
        <replica>ch1</replica>
        <hostClusterType>online</hostClusterType>
        <hostClusterRole>data</hostClusterRole>
    </macros>
</clickhouse>
